#

## Current state

Currently, can track:

- `click` event on:
  - `button` element
  - `a` element
  - `input` element
  - `select` element

Should track:

- `change` event on:
  - `option` element
